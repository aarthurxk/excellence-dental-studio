

## Plano: Botoes "Voltar ao Site"

### O que sera feito

1. **Tela de login (`AdminLogin.tsx`)**: Adicionar um link/botao abaixo do formulario que leva o usuario de volta a `/` (pagina inicial do site).

2. **Painel admin (`AdminLayout.tsx`)**: Adicionar um botao "Voltar ao site" no header do painel (ao lado do titulo "Painel Administrativo"), com icone de seta ou link externo, apontando para `/`.

### Arquivos editados
- `src/pages/admin/AdminLogin.tsx` — link abaixo do card de login
- `src/components/admin/AdminLayout.tsx` — botao no header

Alteracoes minimas, sem impacto em nenhuma outra funcionalidade.

