

## Plano: Remover Timetable da Home (manter componente)

Remover a importação e uso de `<TimetableSection />` do `src/pages/Index.tsx`, mas manter o arquivo `src/components/medico/TimetableSection.tsx` intacto para uso futuro.

**Arquivo a editar:** `src/pages/Index.tsx` — remover import e `<TimetableSection />` do JSX.

