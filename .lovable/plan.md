

## Plano: Reduzir header, manter logo grande

### Mudanças

1. **`src/components/medico/HeaderInfo.tsx`** (header desktop)
   - Padding: `py-5` → `py-3` (voltar ao tamanho mais compacto, logo `h-20` continua)

2. **`src/components/medico/Navbar.tsx`** (navbar mobile)
   - Altura do container: `h-20` → `h-16` (voltar ao original, logo `h-14` continua)

Logo permanece no tamanho atual em ambos os componentes.

